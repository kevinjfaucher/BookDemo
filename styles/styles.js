import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
    },
    form: {
        marginBottom: 20,
    },
    input: {
        borderWidth: 1,
        padding: 8,
        marginBottom: 16,
    },
    list: {
        marginTop: 20,
    },
});

export default styles;
